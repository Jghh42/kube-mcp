apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../deployment.yaml

images:
  - name: ghcr.io/jghh42/kube-mcp
    newName: kube-mcp
    newTag: integration

patches:
  - path: deployment-image-patch.yaml
