apiVersion: apps/v1
kind: Deployment
metadata:
  name: kube-mcp
  namespace: kube-mcp
spec:
  template:
    spec:
      containers:
        - name: kube-mcp
          # The harness loads this exact local image into its disposable cluster.
          imagePullPolicy: Never
